// Leaderboard — rankings table
// Designer's domain.
// TODO: fetch from GET /leaderboard, render table with rank, username, score, badge

export default function Leaderboard({ entries }) {
  return (
    <div className="leaderboard">
      {/* Designer implements this */}
    </div>
  )
}
