// ScoreReveal — animated score reveal sequence
// Designer's domain.
// TODO: number count-up animation, badge reveal, confetti

export default function ScoreReveal({ score, badge, onComplete }) {
  return (
    <div className="score-reveal">
      {/* Designer implements this */}
    </div>
  )
}
