// SessionPage — the main interview session view
// This is essentially what Layout.jsx renders. Kept as a page-level wrapper
// in case we add routing later.

import Layout from '../components/shared/Layout'

export default function SessionPage() {
  return <Layout />
}
